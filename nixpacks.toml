[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install -g pnpm@latest",
  "pnpm install --no-frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm --filter @reclaim/backend prisma generate",
  "pnpm --filter @reclaim/backend build"
]

[start]
cmd = "pnpm --filter @reclaim/backend prisma migrate deploy && pnpm --filter @reclaim/backend start"
